package Iterator_Example;

public interface Iterator {
    public Object siguiente(); //Método que devuelve un objeto
    public boolean tieneSiguiente(); //Método que devuelve un booleano que define si existen más elementos en la estructura de datos
}
